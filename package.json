{
  "name": "script-runner",
  "version": "1.0.0",
  "description": "Node.js app to run scripts daily",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.6.7",
    "express": "^4.18.2",
    "node-cron": "^3.0.2",
    "swal": "^0.1.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
